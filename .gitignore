# macOS
.DS_Store
**/.DS_Store
Icon?

# Editors
.idea/
.vscode/
*.swp

# JavaScript
node_modules/
**/node_modules/
dist/
**/dist/
.vite/
coverage/
playwright-report/
test-results/
*.tsbuildinfo

# Local environment
.env
.env.*
!.env.example
*.log

# Claude Code local state (project skills and launch config stay tracked)
.claude/settings.local.json
.claude/.cc-writes/

# Upstream reference, kept on disk and deliberately out of version control.
# See NOTICE: we hold no rights to it and cannot redistribute it.
toho-like-js/

# Art-kit output. `bun run art:kit` emits paintable templates here; they are a
# working surface for an artist, regenerated on demand, never committed.
art-kit/

# Imported purchased third-party art pack. `bun tools/import-bulletpack.ts`
# derives this tree from the owner's local BulletPack copy. Project/commercial
# use is permitted, but source-sprite redistribution is not granted; therefore
# the ART is not committed — only the importer and semantic mapping are. The
# local pack is regenerable on demand.
packs/bulletpack/

# Throwaway measurement harnesses. These drive the real `Run` to answer a
# question a unit test cannot — "does a player ever reach this", "what does
# tier 3 actually deal" — and they are written, read once, and abandoned. What
# survives a probe belongs in a test, not here.
.probe-*.ts
