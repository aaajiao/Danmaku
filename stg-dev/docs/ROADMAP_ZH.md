# 1bit STG 工业化路线图

状态快照：2026-07-18

目标：把当前 V4 Lab/PWA 基础推进为可验证、可离线、可重放、可发布的完整 STG。本文按证据标状态，不以文件存在代替系统完成。

## 1. 状态定义

- `DONE`：代码、自动化测试和文档均存在，当前分支验证通过；
- `WIP`：已有实现或测试骨架，但尚未端到端/未全部门禁；
- `TODO`：尚无可接受实现；
- `BLOCKED`：有明确外部阻塞和 owner；“工作量大”不算阻塞。

## 2. 当前基础，不是完整游戏

| 能力 | 状态 | 证据与边界 |
|---|---|---|
| Bun/Vite/TypeScript/Three.js 工程 | DONE | Bun 1.3.14 单一入口、精确依赖版本、strict build、Three.js renderer |
| V4 48 pattern 数据入口 | DONE | Content Authority 展开 13 个入口、验证 781 个物理文件/778 个 SHA-256、版本/ID/引用并生成确定性 digest |
| 120Hz authority clock / Lab simulation | DONE（基础） | 默认 RUN 用整数 `tick120` 推进 canonical session；只有 Pattern Lab 保留旧 `GameSimulation` |
| Pattern authority | WIP | NORMAL oracle 的 48/48 trace hash 与 96/96 safe-gap path 精确匹配 V4；生产 `CanonicalCombatKernel` 可直接执行 24/48、剩余 24，exported live-admission registry 仍为20，Clock Decree、No-dusk Grid、Room Threshold 与 Stable Intersection 只在 private direct-kernel tuple。Stable Intersection 锁定双 emitter `lattice`、Python-oracle `A∨B` 双时钟、continuous sine collision mask、`223/300/354` 个 all-identity candidates、tick1488 production 与 tick1867 flake-drain hashes；`intersection_hold_ms` 保持 inert，不进入 composer/session。One Sun、Room Threshold、Clock、No-dusk、Unstable/Alternating、Override、Ballot、Context、Rain、Crack、Dusk 与其它已接 path 保持原有分层证据；FORCED_ALIGNMENT 4/4、IN_BETWEEN 2/4、POLARIZED 4/4、TRANSITION 3/3、weather echo 2/3。Boss observe 为4/8 rigs、4/24 Boss patterns；phase exit/evaluator/resolution/terminal/phase2/3 仍未接。另有彼此独立的 exact caller-resolved Left/Right 与 Alternating READ-only execution slices，但都未接 room selection/composer/live 排程；`boss.two_claims.phase2` 仍 unsupported |
| Canonical Run session | WIP | 默认 RUN 已接 guarded `quiet_awakening → common.eye_acquisition`、V4 Flower/GazeMachine 与来源 drain facts；First Eye occurrence `run:first-eye:0` 使用 shared combat state，released kernel 后由 state 继续 idle timer；三道 handoff barrier 当前不 ready，完整 encounter/narrative phases 未实现 |
| Run Memory | WIP | recorder/validator、ghost 压缩、storage adapter 与单测已完成；彼此分离的 route-present Snapshot/Restore 只接受 recorder-issued frozen token，in-memory Archive 只接受 Snapshot-issued exact receipt。Snapshot 只观察当前 Run，不发 cross-run event；serialize 成功后才铸造 receipt，Archive 只在同一 tick 接受一次并保存原 token；Restore 以三向 shared ledger 保证 previous-run/route/next-run 唯一性。durable storage、app boot、parsed/persisted restore、null-route、renderer 与 IndexedDB 未完成 |
| Keyboard/pointer/gamepad | DONE（浏览器基础） | standard mapping、dead zone、hotplug、edge、可选 haptics；实机矩阵未完成 |
| PWA/图标 | DONE（基础） | RUN manifest、SW、any/maskable 图标、waiting-worker 非运行中接管；Three/frame-index/pattern-manifest 稳定分 chunk，但仍全量 precache；N→N+1 双构建升级回归/存档迁移未完成 |
| Unit tests | WIP | 当前完整 `bun run test:all` 已通过：unit 29/29 files、584/584（combat 176/176）、content 8/8、build 69 modules、smoke 1/1、Chromium E2E 14/14。Stable Intersection 新增7项 focused cases，锁定 exact contract/QA reports、hostile descriptors/private boundary、E/N/H schedules/lattice/RNG、A∨B clock/continuous phase mask、production/full hashes、nonzero start/cadence/backlog/weather/accessibility parity。Room Threshold、Clock 与 No-dusk Grid 各7项、LiveRunAdmission 28项保持；缺其余24 patterns、完整 Run/perf |
| Playwright E2E/smoke | DONE（基础） | production preview 验证 exact V4 boot、显式 seed fail-closed、guarded 序章、因果输入/暂停/保留 backlog、Full/Reduced/Flash-Off fragment trace、warm-offline reload、live collider 与 source drain；离线升级与完整 Run 门禁未完成 |
| Boss/laser/narrative/cross-run | WIP（authority） | 8×3 Boss phase、8 laser、16-state narrative reducer 与 route-present snapshot/in-memory archive/restore 已有单测；archive 精确锚定 serialize tick，`snapshot.complete` 或 archive acceptance 都不会单独授权 handoff，FIRST_CLAMP_RECOVERY 不再绕过 conjunctive Flower guard；Misreader 另有隔离 entry fragment，live producers、默认应用、renderer、null-route 与 durable persistence 未接通 |

因此当前正确称呼是“工业化基础 / playable reference”，不能称为 complete game、release candidate 或 production-ready。

## 3. P0：权威正确与完整可玩

P0 全部完成后才进入 Alpha 候选。

| ID | 工作 | 状态 | 完成定义 |
|---|---|---|---|
| P0-01 | Content index/schema/hash | DONE | 13 入口、40 版本、9 schema、818 ID、781 文件/778 hash/3 exclusion；dev/build fail-fast |
| P0-02 | 120Hz master + 60Hz runtime due-time | DONE（scheduler） | 30/60/90/120/144Hz、large delta、pause、一小时无漂移；V4 machine adapter 仍归各系统 |
| P0-03 | Canonical ordered event bus | DONE（authority） | 72 event、required payload、occurrence 去重、五阶段顺序、只读 feedback；run state 的不可逆 lease 只接受 exact next tick、关闭安静 tick、拒绝 ambient flush/第二 owner 与 accessor/virtual-array reentry；默认 First Eye 已接入，Pattern Lab 仍使用 legacy trace |
| P0-04 | 48 pattern oracle parity | WIP | NORMAL 48/48 trace、96/96 safe-gap、12 operator/13 geometry；生产 kernel 可直接执行24个 pattern，live-admission exported registry仍为20。One Sun、Clock Decree、No-dusk Grid、Room Threshold 与既有 Ballot/Unstable/Alternating/Context/Rain/Crack/Dusk/Notification seams 保持。Stable Intersection 额外锁定 12400ms exact contract、双 `lattice` emitter、Python-oracle `A∨B` clock、continuous sine collision-only mask、`223/300/354` all-identity candidates、QA/tick1488/tick1867 hashes，且保持 direct-only/hook-inert。FORCED_ALIGNMENT 4/4、IN_BETWEEN 2/4、INFORMATION 3/4、POLARIZED 4/4、TRANSITION 3/3、weather echo 2/3；其余24个尚未执行，`boss.two_claims.phase2` 仍 unsupported。Rain parallel member 与 Context/Ballot/Alternating singleton room 只通过 caller-resolved no-bus/no-composer/no-execution admission 校验；Clock / No-dusk singleton 分别以 `43bf1afb…007e2` / `cc6c9636…7307b8` exact拒绝。Stable Intersection 与 Room Threshold 都不是 room-admission candidates；Left/Right 与 Alternating exact READ-only executors 也仍不扩大 live room authority |
| P0-05 | Projectile authority | WIP（authority） | 7-stage、entity-owned flight、swept circle/capsule、固定 pool、generation graze、exact-arm atomic cancel 与 malformed-command fail-closed 已完成；pool/RNG/schedule 是 occurrence-local，默认 First Eye 已组合 spawn→flight→impact/cancel→residue drain，Pattern Lab 仍是 legacy |
| P0-06 | Player/damage/Override | WIP（authority） | leases、稳定多 hit、原子 fatal/non-fatal、respawn/handoff、evidence/graze、局部扇区 swept cancel 与真实 TOI scar 坐标已测；prepared damage 以 owner/bus/tick/revision + exact draft-group receipt 绑定，strict own-data capture、stale/replay/reentrancy、recovery/respawn staging 与 conflict-without-health/lease mutation 均有证据；player/evidence/graze/Override 现由 run state 跨严格串行 occurrence 与 idle tick 保留；dead/respawning/run-ended 锁定 movement/Focus/Override 并投影 V4 稳定代表帧（非 event/tick-bound clip）；默认 fragment 在 local-resistance authority 前不接 Override 边沿 |
| P0-07 | Encounter/room Run | WIP | `CanonicalRunSession` 只装配 quiet awakening、First Eye 与 `first_clamp_recovery` authority slice；combat drain、gaze clamp/release、Flower recovery 分开记录，因 recovery timing 缺失不会 ready。严格串行 shared state 已证明两次 occurrence 的 player/evidence/Override continuity、release-after-close 与 idle timer handoff。`CanonicalLiveRoomExecutionFragment` 只重新验收 Left/Right fixture `b6a1eddf…d1c`，在 metric tick960 之后保留 telegraph+entry 的159 tick elapsed authority，从 `S>=1119` 执行到 `S+1542` neutral close。彼此独立的 `CanonicalAlternatingVerdictReadFragment` 只重新验收 singleton `36da160c…24131`，以同样因果起点执行 `POLARIZED/listen/EASY` 到 `S+1692`；它在 `+1683` 释放 occurrence，pinned stationary-neutral trace 的中间 ticks 无事件，其它合法输入仍可提交 run-owned timer 事件，最后仅在 quiescent 时允许 neutral close。两者都不执行 telegraph/entry，不调用 transition，不接受 terminal gameplay input，且 `roomComplete/handoffReady/runHandoff` 恒 false。Alternating 还明示不拥有 composer、selection/selection RNG、scheduler、parallel、incoming safe-gap、weather、persistence、default RUN/session/renderer 或 future-tick authority；pair `0659e91c…ba820` 仍可 admission，但会在 singleton executor 创建 bus 前被拒绝。其它隔离 Misreader、Ballot、Clock Decree、No-dusk Grid、Crack、Unstable、Context、Dusk、Override Void、Snapshot、Archive 与 Restore capabilities 均未连接 session、room scheduling 或 renderer；Clock / No-dusk candidates `43bf1afb…007e2` / `cc6c9636…7307b8` 在这道边界只得到 `unsupported-pattern`。No-dusk 的 `resolutionHook:"no_dusk_clock_ticks"` 也保持 inert，不自动完成、发事件或写 metric。尚无 room scheduler、parallel/multi-pool fanout、tier runtime budget 或 rollback transaction。纯 QA `RunComposer` 已对齐 Python 16-seed 报告与完整样例，atomic room FSM 已对齐 60Hz due-time；`LiveRunAdmission` 的完整计划仍因 capability 不足整体拒绝。`admitLiveRoomCapability` 的所有 artifacts 与未来 admitted full plan 都固定 bus/composer/execution false，只做 membership/order/seed/segment/capability validation；只有两个 exact adapters 在各自重新验收后创建内部 bus/shared state。520ms 空间 overlap、tier budget、live room-count/difficulty-salt/parallel/segment policy 仍未授权。旧 mixed encounter envelope 已隔离为 no-bus fixture。完整 room/wave/transition/Boss/narrative Run 未实现，不回退 legacy `RunDirector` |
| P0-08 | Boss 与 laser | WIP（authority） | 8 Boss × 3 phase 事件机、8 laser lifecycle/连续碰撞已测；Boss 与 laser 各自的 begin/swap/resolve、active-entry/terminal/cleanup 已做 conflict-atomic batch，standalone 无事件 rupture mutation 已删除。Absent Receiver query 只执行 rig 明示无 laser 的 observe pattern；其 generic entity-drain readiness 不解释 rig `evidence>=1` 与 pattern `phaseEvidence>=1` 的差异，也不提交 phase swap。Misreader `observe → enforce` 则以 exact receipt 完成窄范围 Boss-swap→一次 laser-start；contact 每 generation 至多一次、只伤害玩家、不 impact beam，并保留无 repeat cadence/无 scar 坐标的缺席。但该 fragment 不执行 phase-2 emitters，也没有 phase-evidence evaluator、live cycle、room/session/app/renderer 组合；通用 player-damage→projectile/laser-impact 与 rollback transaction 仍未实现 |
| P0-09 | Narrative/world memory | WIP（authority） | 16 state、64 observations、8 Boss resolution projection，以及彼此分离的 route-present snapshot、in-memory archive 与 material→ghost→residue→witness→input restore authority 已测。Snapshot 在偶数起点 `T` 的 `T/T+50/T+98/T+196` 发 current-run begin/serialize/present/complete，不发 cross-run event，numeric seed 是显式应用 adapter；archive 只接受 serialize 成功铸造的 exact receipt，并在同一 tick 每 run 写一次原 token。complete 或 archive acceptance 都不授权 handoff；route960ms restore 对齐 tick `0/52/166/200/252`，input 持有到 return，ghost 三类均 `NONE`。NarrativeAuthority 不能以 gaze release/room swap 代替 `flower.recoveryComplete`。live session boot、null-route、durable/parsed restore、renderer、IndexedDB 与 app E2E 未完成 |
| P0-10 | Deterministic save/replay | WIP | fragment/authority fixture 已覆盖 seed/input/content digest/trace hash、canonical serializer 与同输入可重放；完整 Run save/replay 未接线 |
| P0-11 | 测试/CI | WIP | type/unit/build、4 V4 validator、E2E/smoke 已自动化；oracle/accessibility parity 与长期 artifact 未完成 |
| P0-12 | 文档/扩展治理 | DONE（基础） | 架构、设计、测试、路线图与 Extension ADR gate 已落地；后续扩展逐项执行 |

P0 发布硬门：0 schema warning、0 unknown operator、0 orphan event、0 fixed projectile flight timeout、0 feedback→gameplay edge、0 accessibility trace mismatch。

新增的 shared adapter 边界同样必须保持显式：occurrence identity 使用一次性 Unicode scalar 与 UTF-8 byte-length prefix；event-bus lease 只授予 exact-next-tick closure。Prepared receipt 只证明同 bus 的 exact draft group 已在一次 append 中受理，不等于跨 authority rollback，也不证明 scheduler、parallel/multi-pool fanout 或 tier budget。

已知的 V4/adapter 边界必须保持显式：`CanonicalCombatKernel` 不替 V4 猜测 graze radius、通用 projectile damage 或 archetype→pool-class mapping，三者仍是带 `application-required-v4-omission` provenance 的应用接线参数。负 aim lead 从精确历史 `tick120` 取样；正 aim lead 暂以最后一段 authority 移动线性外推，该 policy 必须保持可见。`room.forced.left_right_gate` 的 signed lateral drift 与 lane omission 来自 V4 declaration；不可变 Python `sim_core.py` 不执行二者，所以 Python hash 不构成轨迹 parity 证明，只保留为 QA baseline，应用 checksum/tests 封住该差距。`hard_cut_corridor` 同样把 Python QA 与 declared/application 语义分开：report seed `3982869609` 的 EASY/NORMAL/HARD Python intervention 为 `30/42/54`，declared intervention 为 `24/34/39`；应用另锁候选/lane/RNG/preflight/spawn/OOB/end 计数、tick1296 pattern end 与 tick1608 residue drain。`stale_packet_retry` 的官方 NORMAL QA hash 是 `68ea9d2c…1dc14f`，30Hz declared hash 是 `36e6c149…de3c97`；120Hz 应用另锁 curve×envelope、arm、spawn omission、contact 与 tick1176→1654 lifecycle，且保留 EASY 9249.8ms burst 晚于 9100ms marker 的 source tension。`boss.absent_receiver.phase1` 的官方 NORMAL QA hash 是 `1d20801a…a46c42`，E/N/H 120Hz preflight 为 `9/10/3`，tick1296→1583 排空；rig exit 的 `evidence>=1` 与 pattern hook 的 `phaseEvidence>=1` 没有已授权映射，因此不提交 Boss handoff。One Sun 同样把 rig `one_sun_one_rule.evidence>=1` 与 pattern `phaseEvidence>=1` 分开；`laser.single_decree_sweep` 只作 family association，observe phase 固定 `laserGeometry:null`，没有 phase-exit evaluator、resolution/terminal producer或 phase2/3 execution。Clock Decree 的 triangle corridor 只有 cusp-segmented 120Hz application sweep；Python 30Hz `22/33/55` 是删除式 QA intervention，不能替代生产 reversible collision mask。`admitLiveRoomCapability` 的 POLARIZED pair / singleton Alternating / singleton stale hashes 分别是 `0659e91c…ba820` / `36da160c…24131` / `7915d5ce…72c24`，但它们只验证 caller-resolved room facts；Clock hash `43bf1afb…007e2` 则保持 exact rejection。pair 仍可 admission，而 exact Alternating singleton executor 在 bus 前拒绝它。完整与 room admitted artifacts 都固定 bus/composer/execution false。既有 full-Run candidate hash 保持 `61b57932e31d521380219cb1b27357c7198da41ece34aca162423ac56b36b75b` 且仍被拒绝。`boss.unanswering_feed.phase1` 的 visible rule clip 同样以应用生命周期计数锁定，不能把 Python intervention count 当作完整运行时证明。Run manifest 未给出 First Eye room/difficulty 和 difficulty salt，因此序章显式记录 `INFORMATION` / `EASY` 选择，只接收 caller-resolved V4 encounter seed，不自行发明 salt。LiveRunAdmission 要求 raw seed、14 metrics、salt/seed、segment/parallel/Boss facts 全部由调用方显式给出，不能把验收器当 composer。Quiet Awakening 已组合 meaningful-input guard；GazeMachine 只消费显式 device-neutral sample；V4 未定义 Flower recovery timing，所以 handoff 保持 not-ready。四个已接入 Boss pattern（4/8 rigs、4/24 Boss patterns）都只允许 rig `observe` phase 明示的 `laserGeometry: null`；active-laser phase 不得借此视为已接入，`boss.two_claims.phase2` 仍明确 unsupported。`broken_packet_polyline`、`misread_bezier`、`single_decree_sweep` 与 `scrolling_comb` 的 family association 不代表 active laser；laser width/tolerance 只按各自 manifest authority 处理。V4 也没有 standalone Boss rupture canonical ID，所以 material remainder 只随最终 resolution。narrative producer 缺口与 no-bus encounter fixture 仍保持显式。

No-dusk Grid 也必须保留 QA/production 与 admission 分层：不可变 QA deletion `13/18/22` 不等于生产 phase-off 或 lifecycle removal；phase-off 保留 motion/identity，clock-off 才冻结 speed/collision。其 exact live-room singleton hash `cc6c9636b2dd90d8b289d1d68fe7048ea1025c5cf01dea27e6912b047c7307b8` 只得到 `unsupported-pattern`，exported registry 保持20；`resolutionHook:"no_dusk_clock_ticks"` 不是 completion/event/metric producer。

Notification Overflow 使用 report seed `1205726097`。官方 NORMAL reference hash 为 `c4c0c449…39bb2a3`，30Hz declared NORMAL hash 为 `16813e42…56d4ad`；120Hz E/N/H `candidate/lane/RNG/preflight/spawn/OOB/end` 是 `180/0/180/28/152/75/77`、`240/16/224/39/185/106/79`、`288/32/256/46/210/148/62`。Python 冻结 age-zero speed、忽略 arm 与 lateral lane behavior，因此只作 QA；生产按 V4 在每个 120Hz master tick 采样一次 pattern-relative sine pulse，并让 curve-key segments 与 shared swept preflight 共路。EASY `10634.8ms/tick1277` final burst 晚于 `emit.end=10500ms/tick1260`，仍在 `duration=11200ms/tick1344` 内，scheduler cadence 保留；291-tick packet dust 于 tick1635 排空。

Wind Bias 使用 report seed `1709394890`。120Hz E/N/H production event hashes 为 `20ef8b0405aca18abe447409d3452d3ea22bcd29d045f034abbd9691b9545f19`、`c69bb312fa1da427f4980334369f98a7e8c86f39d3757cd00f4153cb896970c3`、`b36d107625c6c1f9f45792108cd6a21293304e708404f8e08200df35d158640b`；四个玩家 room、presentation weather/accessibility、30/60/144Hz 与 retained backlog 保持同 trace。weather echo 现为 2/3；尚缺的是 `encounter.weather_echo.ash_memory` 的 history replay authority。`room.information.missing_ack` 是另一条 INFORMATION pattern，其 split-generation child arm/phase-gate 同样尚未解析，二者都不通过推断接入。

Rain Packets 使用 report seed `1771193663`（manifest base `1771200059`）。它没有 lateral-wall/open-lane；E/N/H 的 `candidate/RNG/preflight/spawn/OOB/end` 为 `140/140/21/119/46/73`、`195/195/29/166/71/95`、`225/225/39/186/107/79`，120Hz production event hashes 为 `869d1ee119a4a772698c27a769386cf02fcca1a06b09521e085e1b2f306a308d`、`afd1dde61a6bc1b076571813e2b065bed8ca536b2d8d947e43cf66f0ff8038c7`、`1829f3539e67f76b8013d73ef10242369f59909d58b7f1b439772db013aca8d9`。reference/declared 30Hz intervention 为 `20/28/35`，只作 QA 分层证据，不能替代 continuous production preflight 的 `21/29/39`。EASY `8885.2ms/tick1067` final burst 晚于 `emit.end=8700ms` 但仍在 duration 内，cadence authority 保留；tick1128 complete、tick1584 drain。测试锁定 nonzero start、graze522/impact533、四 room、presentation weather/accessibility、30/60/144Hz 与 backlog parity；没有真实 weather 或 scheduler 写入口。

Context Switch 使用 report seed `2740011774`（manifest base `2740017633`）。`operator_constraint` 只在 A 的 literal `op.linear > op.turn_once` 与 B 的 linear `op.speed_envelope > op.turn_once > op.linear` 后作为 safe-gap enforcement 执行，不开放成通用 motion operator。adapter 以相对正弦极值分区加固定 52 次二分求连续 first entry，contact 使用 safe prefix + curvature-bound boundary chord，并允许同一 generation 重复 endpoint snap/signed `±8°`；所有候选保留 RNG/identity，`preflight/source_withdrawn=0`。E/N/H 30Hz `emission/candidate/intervention/hash` 为 `19/122/104/43c0ccdeed148b1608137f2db353d90fb89a53361a86a0bc4f263007eadcc30d`、`20/169/154/eaee02492d1be50f8df214f226ffe8be568b89b35085e25e3a9fa4ec5657846c`、`20/198/273/4cc95eb7f32cce086ddf5ff8cee009f4602664dfdb346a09a32f81c534578577`；120Hz `candidate/RNG/spawn/source-withdrawn/OOB/end/residue/redirect-left/redirect-right` 为 `122/122/122/0/75/47/82/93/49`、`169/169/169/0/120/49/99/110/50`、`198/198/198/0/166/32/103/215/121`，event hashes 为 `7cb60b23323a16da617297daec9b3ce437cc1246e56b28f629b3288eff163bb0`、`99a2e087c38cbdd977766c3c3133d3ae8f3c6682ab7ce61f92fce524c0a9a1fb`、`b5a55f8da9c3a317289c8d871a1ad31c2a91973e5f8f923f3350496c37cb2855`。EASY `11398ms` final A burst 与 complete 同为 tick1368；late 6 先保留 identity，再在同 tick canonical ordering 下直接成为 `pattern_end` residue、无 collision-on，tick1746 排空。nonzero start、graze/contact、redirected Override、30/60/144Hz、backlog 与 presentation parity 已锁定；caller-resolved room admission 仍固定 no-bus/no-composer/no-execution，不能称为 live room。

Unstable Middle 使用 report seed `1610616880`。双源 `paired_fan` 均按 literal `op.linear > op.turn_once` 执行：跨过 880ms turn 的 tick 先以旧 heading 完成 motion/sweep，再零时长转 `±16°`，新 heading 从下一 tick 起生效；preflight 使用同一完整转向前后 path。E/N/H `candidate=RNG / preflight omission / spawn / OOB / pattern-end` 为 `144/6/138/94/44`、`180/12/168/137/31`、`216/12/204/188/16`；active residue / allocated / peak live / peak residue / peak bodies 为 `86/96/53/86/96`、`88/125/67/88/125`、`87/166/80/87/166`。pattern-end `events/hash` 为 `1208/1254d8341e630677096392855d1f683854c38056bb128a904e947d674158ced9`、`1504/0463a2a5fc3ce212d6cdf551f8c20127a2dbee2e7ccb5ca66ba36272e79eaf26`、`1866/f2f0ce3506a8fdfadf32888b76f59365fe92c4ef26614d84a1d73cf7a3243fe0`；full-lifecycle 为 `1380/2ffd9cd25098d60ff8033812580d73fa7de1b3c2abacf8a2eb0b64ad2cdc0ff0`、`1680/175dd9006058b797fc652d666d14a86aaf79b0add900a57523f63652f65ad44b`、`2040/47990c6f57c3492a9a4b03c311137346c12109e5e574fa112db54b7dacbbb052`。tick1392 complete、tick1807 drain；三难度均 `source_withdrawn=impact=damage=0`。这只是已有 room capability 的 declaration-order 修正，不启动仍 unsupported 的 `boss.two_claims.phase2`；加入 Stable Intersection 后当前总覆盖为24/48。

Alternating Verdict 使用 report seed `4224146597`。A/B 的 `op.linear > op.turn_once` 在 crossed tick 先以旧 heading sweep，再零时长转 `+32°/-32°`，下一 tick 以新 heading 移动。angular omission 顺序是 source index → 一次 RNG jitter → full declaration-order swept preflight → identity/spawn；被省略项没有 event/identity/residue，已通过者的不可能 corridor 违例 fail-stop 且绝不转成 `source_withdrawn`。E/N/H `RNG / omission / spawn / OOB / end` 为 `162/12/150/99/51`、`198/15/183/147/36`、`234/15/219/201/18`；pattern-end event/hash 为 `1334/0b6cba1906a6c172c8fa68d34b3f47595edf1756847e94850dec2d1f18d773eb`、`1672/62ef4cbd2fe769fc53d8653c6010c4987f0bd5652e32c6e3d4a0c4e66e78b02b`、`2050/d3839818426b980b0bb14e6851091d0aae29a4eda00e1efffcab1bde28aa736f`；full-lifecycle 为 `1500/b7f2b9bca9fd76bce42f245cfd4cae302aec8297c19a836e6b38ad4e46e77a7f`、`1830/25a0fdd4617d491a33aa6fa9502af447dc5e6103582844c16ab9a81fddd22969`、`2190/92a7f8055a6703289bae5e570d7e07583cc5c8c7fbf7ade38c5a3e2b7a9c4c87`。tick1392 complete、tick1683 drain；三难度均无 `source_withdrawn`/impact/damage。该修正本身不扩大 POLARIZED；Clock Decree 与 No-dusk Grid 仍将 POLARIZED direct-kernel coverage 保持在4/4，加入 Stable Intersection 后总覆盖为24/48。

Dusk Settle 使用 report seed `924053617`。E/N/H reference hashes 为 `216fcd54ba5b35eb60786f245a17fdc9445695f064e77cda8bce69739bc2469a`、`18e63cdf2b880d71a5dd3f84680464d2d047e14bf7a144357e5024925a846298`、`5c88aa2503048653ded017733a829f94deb3516165cb0a202101602808824988`，120Hz production event hashes 为 `9867ff2413c15077d0ad7b5d487a65384de3f47be2d11de791f26b8bfe35776a`、`cbc7a1f93f73a9e6a68fa59fd34b4a91923bbcaa4db1c0b4d22ee1b2339797df`、`97dc90a77e51700d393f90d72ef4988964d4c1a7e89dc8342a876186f5b6289f`。startTick401 与零起点的相对 motion/lifecycle/normalized events 相同；四个 mental-room context 与 30/60/144Hz/backlog 两个轴分别 trace-identical。source hook `snapshot_capture_ready` 保持 inert，所以这不是 live transition 或 snapshot 接线证据。

Override Void 使用 report seed `1930559651`。E/N/H 四次 full-360 ring 生成 `48/64/76` 个 identity，visible rule clip 以 `source_withdrawn` 撤回 `2/3/4`个；每个 generation 只在首次 inclusive seam crossing 施加一次 signed `±22px` offset，并把 linear 与 offset-discontinuity 两段 sweep 交给 contact、rule clip 与明示 DirectionalOverride。E/N/H tick912 complete hashes 为 `193dbe2b90324ea80cef276e003262e6f805fd169fb21a0b0a20e717208d769b`、`0222f436df16b75e04792e9cf9c5000ab5e31ffe6716a390a33994928ddcce43`、`6cbfe5b2fbd821b78ef746b536aab27bddb04852c604519dd7bd0f2c496d7e51`；tick1258 drained hashes 为 `f00bd4fedcb6aae210bf6a04a8c19db2179442dbffb59d6671da2323569303f8`、`5c62bd0bca3d7ce1da8f969de9fe8354c68ae83a0c6754a592cfea74fdb0c251`、`6709ae5aa9dcaa970aef8a25e85c05556a7de05cf6fea6cae5f56c6cdd38057d`。同 tick rule clip 优先以 collision-off/cancel/residue 终止，不产生第二 Override terminal 或联动 scar；`scar_coordinate_commit` 保持 inert，只有另行启动的 DirectionalOverride 周期写入真实取消坐标。这 7/7 项专项证据仍不是 `evidence>=overrideCost` phase admission、director/session/composer/scheduler、room transition/event、automatic hook completion/persistence/Snapshot/handoff、renderer 或默认 RUN 接线；无新 asset、event ID、dependency 或 gameplay language。

Crack Fall Loop 使用 report seed `3074674485`。E/N/H 120Hz production event hashes 为 `7b3e9adcc1b71329e5df851949d376a3f02cc19b51613e9648429af61d6496b8`、`22af3f70c7f67589db56e98ac7fc4a3f4e2f1d4c0d1dd212fbf4b73ad4d1ac48`、`2ca79d498e7b0a53a51993ec23974a652f726d50f137011695fd0c807289a2f4`。应用锁定 generation-owned inclusive seam mirror、pre-mirror linear 与 zero-time discontinuity 顺序、analytic-extrema + 52-step bisection corridor entry、曲率界 boundary chord，以及 exact-owner/current-tick prepared Override path 的真实 first-hit scar。Python 只授权 endpoint edge snap 与 signed `±8°` redirect，不构成 continuous 120Hz path parity。E/N/H candidate/RNG/spawn 为 `90/90/90`、`120/120/120`、`140/140/140`；tick1320 完成、tick1782 排空。这不是 live FORCED_ALIGNMENT room activation 或 scheduling 证据。

Ballot Shift 使用 report seed `1912173942`。pattern-global 整数 `tick120` dual-clock XOR 关闭时将同 generation 的速度与碰撞同时置零；开启边界的 collision-on tick 仍静止且 contact-ineligible，下一 tick 才恢复移动。另一道 continuous lane `phase_gate` 保留 RNG、identity、generation 与 linear motion，仅屏蔽走廊侵入时的碰撞；masked body 仍可被 Override 精确 cancel/scar。prepared transition batch 先预验同 tick 全部 collision leases，再一次 append 与完成 after-state，不让 duplicate key 或后续 contact fault 把已接受事件与 projectile state 分离。Python 30Hz E/N/H deletion `26/33/55` 只作 QA evidence；生产保留全部 `170/220/260` 个 candidate/RNG/entity identity，event hashes 为 `4ed653e2f043eddd47c3488bae6428c7ddcd3d9c0a6015cda2f7bfca692548fb`、`7d15af539bf24e1da5174ac29abbd4f81f2adbd018b9017b776a17921f097d3b`、`54c5ddcebe8adb79e603478ee1fa20a9cf03b744297bb04e01cc797b2b3d763f`。tick1440 complete，310-tick `seam_filament` 在 tick1750 排空。FORCED_ALIGNMENT 只是达到 4/4 isolated capability；singleton admission hash `fea078a46315927d2f145be380ad7f38e6cbfef154e95337fd1ac9c90dcdc2a7` 仍固定 no-bus/no-composer/no-execution，不是 live room 或完整 Run 证据。

Clock Decree 使用 report seed `1517218079`（manifest base `1517220356`）。`dual_clock_gate > linear` 逐字执行：`1000/2000ms`、各 `0.5` duty 的 XOR 关闭时把速度置零并撤回 collision lease，开启边界仍静止、下一 tick 才恢复移动；独立的 `quantized_step/phase_gate` 以中心 `180→234→180→126→180`、cusp ticks `120/360/600/840/1080` 的分段三角路径连续屏蔽碰撞，但不撤回 generation、不冻结 linear motion。30Hz E/N/H `emission/candidate/deletion/hash` 为 `17/153/22/ddbfbf02011fc16e53117e66702bdd8b544f8124bb67e93e8bb1aad4300c0411`、`18/216/33/895b02be0bf75752221ae54ec4ac2d1ef4bf8637f744b32d760e35bbec06f450`、`18/252/55/6a4a588f6f2f7ef6efe55c24b26d9400b318691de1cc6be4fc44fbb7d0358ed0`，只作删除式 QA；120Hz 在 tick1200 的 E/N/H `candidate=RNG=spawn=armed / collision-on/off / dual-mask/phase-mask/OOB/end / active-residue/removed` 为 `153/408/552/379/20/9/144/153/0`、`216/616/784/537/31/48/168/216/0`、`252/787/955/654/49/84/168/238/14`，event hashes 为 `364c95cebf91b115de2238b7bbaefa647b88a07892dc8242dcf25cefe70fe06e`、`bde5e39ebc67f11c95a675792af154e219ba44b7efc35e62865d4e7cc74249ba`、`14962808bf7d5003ade192815addd6e1bfce394c6d05a33ac6562371bc7e5911`。EASY 的 `9856ms` late burst 仍在 tick1183 spawn、1188 arm，但因 XOR 已关闭而不位移；HARD 允许 8 次 graze/evidence、三难度均无 impact/damage。`binary_chip` residue 在 tick1493 排空，full-lifecycle hashes 为 `45074fc107311af97af8c7f7c478ff0a985af0cf40121cf5f9392a28a9f5999c`、`4732a1497d743ab9cf896c4a6774762fcad84fe6bcfd99198c527ed3ac6f2bd5`、`ea15a504c83f6755324beb1930db3e59c3d0875b77c84b5d55f666d9bd34ad9f`。这只是 private direct-kernel 第21项 capability：不进入 exported 20-pattern registry，也不拥有 composer、scheduler、selection、live admission、READ/session、room completion、renderer 或默认 RUN；exact singleton hash `43bf1afb9a26ccbe5430a013e66feabf63d481b55d303aee328a237c192007e2` 必须以 `unsupported-pattern` 拒绝。

No-dusk Grid 是 private direct-kernel 第22项 capability。两个 emitter 各自拥有 XOR clock；`binary_cross` safe gap 以 cusp-segmented continuous phase mask 检查每段 motion，phase-off 只屏蔽碰撞并保留 motion/identity，clock-off 才冻结 speed/collision。E/N/H candidate 为 `133/168/203`；immutable QA deletion/intervention/hash 为 `13/e587211cb50d6e42a0feab07f08d18520188495314743e53cc2f79c189315bcd`、`18/b2c402fd550d19386c096ca39f3bf40e12f63fb64080e3d4660acbbdfc49b3f6`、`22/9871c0383df928b0c2f8594380e9295a31f88e30f6bbce0b440084a3947eba57`。production tick1464 的 `activeResidue/removed/allocated/peakLive/peakResidue/hash` 为 `119/14/124/90/119/3ddd331ca7e8a6da50fbd6e863743c58c21f1aab2c541be0f69b14e765b8987d`、`148/20/159/129/148/c9023f0f7ea2ab512901db451990f41fb9f07b0cb2aa845762d02af906243e61`、`161/42/189/154/161/e465928f2680f83cb53009da3f1b3895bee873f7bad3db33c792ac3282bcdfa5`。此时 material residue 仍在 drain；tick1780 尚未排空，tick1781 才 lifecycle drained/handoff-ready，E/N/H hashes 为 `88ba6f54861d98819fae1ee0dba79dae9df1b27d4826b67aacd224b0a17bc1c6`、`aa941a85fd21c0c855d9bcb4a2cf1952ea088dc058d5b6e09ef8ec4b9c06a221`、`3d50e7891159a3ab2d5146270796273c48b56cad8929a950db1e383325dbaf61`。EASY authored `11861ms` 的末次 vertical burst 在 `emit.end` tick1380、`residue.commit` tick1414 后于 tick1424 spawn、tick1429 arm；clock 保持关闭，同位置/identity body 到 pattern end 才 cancel。`resolutionHook:"no_dusk_clock_ticks"` 保持 inert，不自动完成、发事件或写 metric。它不进入 exported 20-pattern registry，也不拥有 composer/scheduler/selection、READ/session/room completion、Boss/laser/resolution、renderer/default RUN；exact singleton hash `cc6c9636b2dd90d8b289d1d68fe7048ea1025c5cf01dea27e6912b047c7307b8` 必须以 `unsupported-pattern` 拒绝。

Room Threshold 是 private direct-kernel 第23项 capability。exact contract 锁定 7800ms、timeline ticks `[0,89,89,468,852,886,936]`、departing line `8×128px/s` + `1→0.55` envelope/6 bursts、arriving fan `6×146px/s` + `0.55→1` envelope/5 bursts，以及 continuous sine `threshold_bridge` collision-only mask。E/N/H candidate 为 `61/78/89`；QA hashes 为 `46f93363fad94f5e8df59e793844758737cbbe0887e63a95d4228fd9692b4c8e`、`0c483797777dc1fcdb1102982ad58d618422f0acd25729f892e2d93f45b42c8c`、`7f2600cfa4ca0a2cceb26dcdcb7759c032290e7bd0e878b8d9759270a2d77aff`。tick936 production hashes 为 `36e3b5c511d7a42d46dfef94c54a0cced93d9392800351e0cc3228ecf378a6ae`、`92d7ea69574d22a1cfe8c1b2fd3fdd07a28293028358d221140ed163dfdb07d5`、`c48b285bdb2fe89b4fe593c73f45d8921784d43d6ff0899ef5b7c091ec7d9e59`；2741ms sediment 投影为329 ticks，tick1265 full hashes 为 `dc6a4490094f45d876b10535843dbb3734548fd19fa0d6125b4878ab2da7825b`、`ebba622fef62ef1b1568552d903134159ec1a4558e13478b90b131b96e9168b9`、`f63a5bc9a19083154b1b69fb95a47ce0186c124e3e496f4646fd6d9cc5e5c072`。该 pattern 没有 resolution hook/laser，不进入 exported registry、atomic `RoomTransitionAuthority`、composer/scheduler/selection、session、room completion/handoff、renderer/default RUN；TRANSITION 3/3 只是 isolated pattern coverage。

Stable Intersection 是 private direct-kernel 第24项 capability。exact contract 锁定 12400ms、timeline ticks `[0,82,82,744,1404,1438,1488]`、`orthogonal-a` 的 12-body horizontal lattice 与 `1600/2400ms` clocks、`diagonal-b` 的 10-body diagonal lattice 与 `2400/1600ms + 400ms` clocks。`dual_clock_intersection` 沿用 V4 Python 公式 `A != B || (A && B)`，有效门为 `A∨B`；clock-off 冻结 speed/collision，clock-open 边界只恢复 lease，下一 tick 才移动。continuous sine phase mask 用解析极值/二分检查完整 motion，只撤回碰撞。E/N/H candidate 为 `223/300/354`；QA hashes 为 `1f5e7cecbfc7dfd3edb0813eee1489deda66b68b862e295445dd845db650aef5`、`5dba80afbfc2c9e1b835d3bca1e13237b23de32d79706597fb8396b708b7fd83`、`ef17e7df7416b4b33a78dceda0b5b4945dfa6c488d2a0420cd3088aecb6e44f8`。tick1488 production hashes 为 `0dab165f009b39c96b804efcc6393e0d3dfac8ab32249f5758fc854bf45d0271`、`44b29fead5702979827b26c15b8a86258076f01b7ab6941b87d8b639d9bf1a08`、`07faaeee0e34dc2287876c7af885056b8515f7d5feb4d4db5ebb5bc41e102b84`；3155ms flake 投影为379 ticks，tick1867 full hashes 为 `35c45ab634ddca50cd67b62b3b80e094bef593d05d0493c024f32855c9acaa15`、`1509b2cf295b6a56a346cda4eaf2bbcf3f62fd30fd75aa319aa73be66b2f69b4`、`31b8b615cc392fc0c0ceb01a2951137c1c6c629e0ea3c2c886fb6c4cd5c2e840`。EASY late burst tick1485 materialize、arm tick1490，因此 tick1488 pattern-end cancel 且从未 collision-on。`intersection_hold_ms` exact-validated/inert；不进入 exported registry、composer/scheduler/selection、live admission、session、room completion/handoff、renderer/default RUN；IN_BETWEEN 2/4 只是 isolated coverage。

Left/Right READ execution 使用 raw seed `0x12345678`、salt `0x1100`、resolved seed `0x787634f1`、`listen/EASY` 与 admission hash `b6a1eddf043960a43a3b2af99cadb355932b6ae26fafb9da1563232a642d2d1c`。metric snapshot 在 tick960 捕获；telegraph520ms + entry800ms 的 elapsed authority 向上投影为159 ticks，故执行起点必须满足 `S>=1119`。READ-relative boundaries 为 material settle/rest/residue/fixed close `+1224/+1350/+1540/+1542`。最早因果起点的 neutral trace 有 860 events、86 spawn、99 RNG，hash `0dceca99986974893345ce2d80e7aff31640e3bd551f98835223be2403016695`。budget observation 为 active digital/live `56/56`、all-authority/residue/allocated-micro `77/74/77`，累计 spawn86；tier80 的 concurrent/cumulative/residue-inclusive 解释未被 V4 创作，因此生产不执行 budget gate。telegraph/entry、incoming safe gap、selection RNG、room transition/completion/handoff、parallel 与后续 tick ownership 都不在此切片内。

Alternating READ execution 使用 singleton hash `36da160cd1a63e96a71c6c5978c1d3b73398e177c8b447ef08274c6215824131`，不从 pair `0659e91c3a0cabbf17a5a5961189d47f13f1a27e341360ad92aca34a674ba820` 中选择 encounter0；pair 仍可作 admission artifact，但在 singleton executor 创建 bus 前被拒绝。fixture 锁定 raw seed `0x12345678`、metric tick960/14 metrics、salt `0x2200`、resolved seed `0xe9f333c4`、parallel-none selection seed `0x1234ba38`、`listen/EASY` 与 `520/800/11600/900/1600/520ms`。执行起点必须 `S>=1119`；READ-relative material settle/rest/residue/fixed close 为 `+1392/+1500/+1683/+1692`。pinned stationary-center neutral trace 的 `+1684…+1691` eventless；其它合法输入可提交 run-owned timer 事件，且 timer 未静止时最后的 neutral close 必须拒绝。stationary-center/unfocused/graze10/damage1 的 1500-event trace hash 为 `21c28e87ea9bdb9fd2a9777fd8f6cc3392209ae5557ede1b766b6d3bcf36bd3c`；digital/live/all-authority/residue/allocated 峰值 `52/52/83/83/83`，spawn/RNG/omission `150/162/12`。composer listen `maxProjectiles=80/maxEmitters=2` 与 director EASY `maxProjectileBudget=120` 只是并置观察；V4 未创作 concurrent/residue/cumulative 计数解释，因此无 budget branch。该 fragment 不拥有 composer、selection/selection RNG、scheduler、parallel、telegraph/entry 执行、incoming safe-gap、transition、room completion/handoff、weather、persistence、default RUN/session/renderer 或 future-tick authority，不发伪 segment event。

Misreader 隔离 adapter 的缺口同样不得被吞掉：入口只接受 canonical catalog 的 `observe/enforce` 绑定，但 `misreader.evidence>=1` 由 caller assertion 授权，当前没有 evaluator/receipt。相对 `S` 的 arm/collision-on/first-contact/shutdown/residue/cleanup 是 `+104/+151/+152/+264/+286/+366`；16 个 capsule、每 generation 一次非终止 contact、difficulty-invariant trace 是应用 adapter policy，而 repeat cadence 与唯一 Override scar 坐标保持缺席。

## 4. P1：生产硬化与跨设备发布

P1 全部完成后才进入 Beta/Release Candidate 候选。

| ID | 工作 | 状态 | 完成定义 |
|---|---|---|---|
| P1-01 | 完整视觉绑定 | TODO | 7 atlas/448 frame、16 reaction overlay、4 room 背景与 fallback 全引用验证 |
| P1-02 | Feedback/accessibility graph | WIP | 序章 Full/Reduced/Flash-Off 同 trace、flash-off 稳定边界已测；完整 Run 的 audio/visual/haptic/UI graph 与 fallback 未完成 |
| P1-03 | 音频图 | WIP | 48 WAV 绑定、room bed、ducking、unlock、丢失 cue 降级、资源预算 |
| P1-04 | 工业手柄 | WIP | Xbox/DualSense/Switch/通用/移动实机矩阵；remap profile；连接状态诊断 |
| P1-05 | IndexedDB + PWA | TODO | versioned migration、atomic archive、quota/损坏恢复、离线 N→N+1 更新事务 |
| P1-06 | Renderer 性能 | TODO | sprite/projectile pool、texture/material 生命周期、draw-call/heap/GPU benchmark、10 分钟 soak |
| P1-07 | UI 与可访问性 | WIP | RUN/LAB 边界、键盘 focus、390px、文本/非闪烁替代、诊断与导出 |
| P1-08 | Release engineering | TODO | reproducible build、Git/content/extension digest、artifact signing/checksum、rollback notes |
| P1-09 | 浏览器/设备矩阵 | TODO | Desktop + Mobile PWA 在线/离线/后台恢复/弱网，风险有 owner 与期限 |

P1 只做生产所需硬化。新的房间、敌人或装饰内容不能挤占权威/性能/持久化缺口。

## 5. P2：有基准后再做的工具

P2 不应提前进入主运行时：

| ID | 工作 | 进入条件 | 约束 |
|---|---|---|---|
| P2-01 | Pattern/trace inspector | P0 oracle 已稳定 | 只读；不得成为第二套权威 |
| P2-02 | Manifest authoring/preview | schema/content index 已稳定 | 产物必须走同一 validator/Extension ADR |
| P2-03 | Deterministic replay viewer | canonical replay 已稳定 | viewer 不修正或补写 trace |
| P2-04 | Privacy-preserving telemetry | 有具体发布决策问题 | 默认关闭；记录行为事实而非玩家 rank；可导出/删除 |
| P2-05 | Worker/WASM 优化 | profile 证明主线程为瓶颈 | 同 trace hash；无共享时序隐患；可回退单线程 |
| P2-06 | 多 renderer/实验 projection | 核心/投影依赖门禁完成 | 不复制 simulation，不改变 accessibility parity |

## 6. 里程碑

### Alpha：完整权威闭环

- P0 全绿；
- 固定 seed 可从 Awakening 跑到 Snapshot/Archive，再进入下一 Run restore；
- 48 pattern、8×3 Boss、8 laser 和 16-state narrative 可执行；
- 无 score/rank/moral ending；行为与材料 ledger 可检查、可重放。

### Beta：生产环境闭环

- P1 全绿；
- PWA 在线/离线/升级/迁移通过；
- 性能 soak 和实机手柄矩阵有归档证据；
- 发布包能报告 Git/content/extension digest。

### Release Candidate：只修阻断

- 冻结内容 digest；
- 只接受安全、数据损坏、确定性、崩溃、可访问性和发布阻断修复；
- 每次修复重跑完整验收并生成可回滚 artifact。

## 7. 开发流与提交边界

建议按可验证垂直切片提交，而不是按文件类型堆积：

1. `chore(content): ...`：schema/index/hash 与 fixture；
2. `feat(authority): ...`：clock/event/order，附 parity tests；
3. `feat(gameplay): ...`：一个可闭环 operator/lifecycle/Boss slice；
4. `feat(narrative): ...`：一个 state + material record + restore test；
5. `test(e2e): ...`：对应用户路径；
6. `docs: ...`：同步 ADR/验收/迁移。

每个提交都必须能独立说明权威变化、验证命令和回滚范围。不要把 asset kit、运行时重构、生成素材和文档塞进一个无法审查的提交。V4 外扩展提交必须带 [CONTENT_EXTENSION_ZH.md](./CONTENT_EXTENSION_ZH.md) 的 ADR/provenance。
