/**
 * Compatibility entry point for the compiled v4 campaign.
 *
 * Pack validation/injection machinery remains under `src/packs`; the authored
 * four-stage campaign and its replay fingerprint belong to `src/v4/content`.
 */

export { BASE_PACK_NAME, CONTENT_FINGERPRINT } from '../v4/content';
