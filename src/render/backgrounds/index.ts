/**
 * Compatibility entry point for the active edition's compiled scenes.
 *
 * The renderer and registry remain under `src/render`; authored shader scenes
 * belong to v4 and register through this historical import path.
 */

import '../../v4/backgrounds';
